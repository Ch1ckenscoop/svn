#summary Sets tesla trap damage.
#labels D-Cvar

= Description =

Sets the damage per shock for the tesla trap.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `5.0` || [FCVAR_CHEAT] || [Float] ||