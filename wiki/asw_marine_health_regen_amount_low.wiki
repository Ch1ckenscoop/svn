#summary Low health healing amount.
#labels D-Cvar

= Description =

Heals marines by this amount when their health is below the value set in [asw_marine_health_regen_threshold].

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `10` || [FCVAR_CHEAT] || [Int] ||