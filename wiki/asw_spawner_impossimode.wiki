#summary Sets all spawners to infinite.
#labels D-Cvar

= Description =

Forces ALL spawners to be infinite. I don't recommend enabling this unless you have fairly small pruning settings and you have [asw_carnage_min_interval] set to a fairly high amount.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r150 || `0` || [FCVAR_CHEAT] || [Bool] ||