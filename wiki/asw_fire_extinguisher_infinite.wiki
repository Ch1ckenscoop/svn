#summary Infinite fire extinguisher.
#labels D-Cvar

= Description =

Prevents fire extinguisher from using ammo.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `0` || [FCVAR_CHEAT] || [Bool] ||