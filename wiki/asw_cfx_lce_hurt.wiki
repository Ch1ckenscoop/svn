#summary Adjusts LCE effect threshold.
#labels D-Cvar

= Description =

Threshold of marine health at which local contrast enhancement effects start to show.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r385 || `35` || [FCVAR_CHEAT] || [Int] ||