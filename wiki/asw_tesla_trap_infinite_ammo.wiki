#summary Prevents tesla trap from using ammo.
#labels D-Cvar

= Description =

Prevents the tesla trap from using ammo.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `0` || [FCVAR_CHEAT] || [Bool] ||