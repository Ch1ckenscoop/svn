#summary Controls queen parasite count.
#labels D-Cvar

= Description =

Sets the maximum number of live aliens a queen can have.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `5` || [FCVAR_CHEAT] || [Int] ||