#summary Forces eggs to burst when this close.
#labels D-Cvar

= Description =

Forces eggs to burst if they're this close to a marine.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r333 || `120.0` || [FCVAR_CHEAT] || [Float] ||