#summary Sets the freeze amount for the flamer.
#labels D-Cvar

= Description =

Sets the freeze amount and freeze time for the flamer's secondary fire.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r211 || `0.4` || [FCVAR_CHEAT] || [Float] ||