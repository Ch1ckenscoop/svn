#summary Makes sentries invincible.
#labels D-Cvar

= Description =

Prevents sentries from taking damage from aliens.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `0` || [FCVAR_CHEAT] || [Bool] ||