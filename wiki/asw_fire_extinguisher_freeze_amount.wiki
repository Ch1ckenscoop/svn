#summary Adjusts fire extinguisher freeze strength.
#labels D-Cvar

= Description =

Adjusts the how long and how fast aliens are frozen.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `0.4` || [FCVAR_CHEAT] || [Float] ||