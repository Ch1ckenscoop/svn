#summary Sets tesla trap ammo.
#labels D-Cvar

= Description =

Controls tesla trap ammo (number of shocks before self-destructing). This value is ignored if [asw_tesla_trap_infinite_ammo] is enabled.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `30` || [FCVAR_CHEAT] || [Int] ||