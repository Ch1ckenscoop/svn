#summary Sets starting number of flares.
#labels D-Cvar

= Description =

Sets the starting amount of flares.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `15` || [FCVAR_CHEAT] || [Int] ||