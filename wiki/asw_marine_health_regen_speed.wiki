#summary How often to heal marines.
#labels D-Cvar

= Description =

This cvar sets the delay between heals. Every heal, it adds the value of [asw_marine_health_regen_amount] to each marine's health.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `0.5` || [FCVAR_CHEAT] || [Float] ||