#summary Sets the ranger's firing rate.
#labels D-Cvar

= Description =

Sets the firing rate for rangers' spit attack. 

Note that even if you set this to 0, the ranger still has to play an animation before it can fire. So this value becomes irrelevant below ~1.5.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r259 || `4.0` || [FCVAR_CHEAT] || [Float] ||