#summary Controls medkit heal amount.
#labels D-Cvar

= Description =

Controls the healing amount of medkits for non-medics.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `50` || [FCVAR_CHEAT] || [Int] ||