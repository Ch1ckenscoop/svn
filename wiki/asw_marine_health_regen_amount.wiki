#summary Amount to heal marines.
#labels D-Cvar

= Description =

Amount to heal marines every [asw_marine_health_regen_speed].

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `1` || [FCVAR_CHEAT] || [Int] ||