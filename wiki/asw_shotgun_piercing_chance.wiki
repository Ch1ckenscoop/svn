#summary Base pierce chance for shotgun.
#labels D-Cvar

= Description =

Controls the likelihood of a shotgun pellet piercing an alien.

Requires [asw_shotgun_piercing] to be higher than 0.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r7 || `0.2` || [FCVAR_CHEAT] || [Float] ||