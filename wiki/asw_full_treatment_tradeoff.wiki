#summary Adds more aliens in syntek_hospital.
#labels D-Cvar

= Description =

Removes all env_sprite entities in Full Treatment to allow for more aliens.

|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r297 || `1` || [FCVAR_NONE] || [Bool] ||